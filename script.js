/* TravelClaw — minimal enhancements */

(function () {
  const el = document.querySelector('[data-updated]');
  if (!el) return;

  // Keep author-controlled timestamps, but allow easy future automation.
  // (No-op for now; placeholder for potential build-time injection.)
})();
